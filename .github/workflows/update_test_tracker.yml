name: 🧪 Update Test Tracker

on:
  push:
    branches: [main]
    paths: ['test/**', 'lib/**', 'scripts/update_test_tracker.dart']

jobs:
  update:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/cirruslabs/flutter:3.22.1

    steps:
      - uses: actions/checkout@v3
      - run: flutter pub get
      - run: dart scripts/update_test_tracker.dart
      - run: |
          git config user.name "AniBot"
          git config user.email "anibot@anisphere.dev"
          git add docs/test_tracker.md
          git commit -m "🤖 Mise à jour automatique du tracker de tests" || echo "Pas de modifications"
          git push
