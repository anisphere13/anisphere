name: 🧠 Update Suivi Noyau

on:
  push:
    branches: [main]
    paths: ['lib/modules/noyau/**', 'scripts/update_noyau_suivi.dart']

jobs:
  update:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/cirruslabs/flutter:3.22.1

    steps:
      - uses: actions/checkout@v3
      - run: flutter pub get
      - run: dart scripts/update_noyau_suivi.dart
      - run: |
          git config user.name "AniBot"
          git config user.email "anibot@anisphere.dev"
          git add docs/noyau_suivi.md
          git commit -m "🧠 Suivi du noyau mis à jour automatiquement" || echo "Pas de modifications"
          git push
