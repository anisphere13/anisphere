/// Copilot Prompt : Test automatique généré pour cloud_admin_notifier.dart (unit)
import 'package:flutter_test/flutter_test.dart';
import '../../test_config.dart';
import 'package:anisphere/modules/noyau/services/cloud_admin_notifier.dart';

void main() {
  setUpAll(() async {
    await initTestEnv();
  });
  test('cloud_admin_notifier fonctionne (test auto)', () {
    // TODO : compléter le test pour cloud_admin_notifier.dart
    expect(true, isTrue); // À remplacer par un vrai test
  });
}
