name: anisphere
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🐝 Local database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 🔧 Base utils
  provider: ^6.1.2
  path_provider: ^2.1.5
  flutter_secure_storage: ^10.0.0-beta.4
  connectivity_plus: ^6.1.3
  http: ^1.3.0
  flutter_image_compress: ^2.4.0
  crypto: ^3.0.3

  # 🔥 Firebase
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8
  firebase_storage: ^12.4.6
  firebase_messaging: ^15.2.6
  firebase_data_connect: ^0.1.5
  uuid: ^4.0.0

  # 🔑 Auth
  google_sign_in: ^6.2.0
  sign_in_with_apple: ^7.0.1

  # 🌐 UI / WebView
  webview_flutter: ^4.8.0
  flutter_local_notifications: ^19.2.1

  # ✅ Ajouts pour module Identité
  qr_flutter: ^4.1.0
  google_mlkit_text_recognition: ^0.15.0  # Mise à jour vers la dernière version officielle
  google_mlkit_commons: ^0.11.0  # Mise à jour vers la dernière version officielle
  image: ^4.3.0  # Modifier la version d'image
  pdf: ^3.10.4
  printing: ^5.12.0
  image_picker: ^1.0.7
  mobile_scanner: ^6.0.0

  # 📱 Capteurs smartphone & IA locale
  sensors_plus: ^6.1.1
  battery_plus: ^6.2.1
  pedometer: ^4.1.1
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0  # Version compatible avec hive_generator 2.0.1
  build_runner: ^2.4.6
  hive_test: ^1.0.0
  flutter_lints: ^4.0.0
  fake_cloud_firestore: ^3.1.0
  # 🧪 Code generation
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

